# Wally 패키지 (wally.lock으로 복원 가능)
Packages/
ServerPackages/
DevPackages/

# Studio 바이너리 파일
*.rbxl
*.rbxlx
*.rbxl.lock
*.rbxlx.lock

# OS 생성 파일
.DS_Store
Thumbs.db
desktop.ini

# 에디터 · 개인 환경 종속
# extensions.json은 팀 공용 권장 목록이라 제외하지 않는다
.vscode/*
!.vscode/extensions.json
.idea/
*.code-workspace

# Rojo 생성물
sourcemap.json

# 세션 로그 (Rojo 생성물 아님. 위 블록과 함께 지우지 말 것)
# 세션을 넘어 살아야 하는 것은 docs/PENDING.md 로 간다 — CLAUDE.md 참조
HANDOFF/
