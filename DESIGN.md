# 게임 설계서

## 코어 컨셉
2트랙 구조. 힘은 방치로 키우고, 블럭스는 능동 챌린지로 번다.

```
[힘 트랙 — 방치형]
클릭 / 자동 → 힘 증가 (배수 적용)
        ↓ 힘을 환전
[블럭스 트랙 — 능동]
20초 챌린지 → 블럭스 획득
```

---

## 1. 챌린지 (핵심 루프)

```
스테이지 N 진입 → 20초 타이머 시작
  → 블럭 K개를 전부 파괴하면 클리어
  → 진행 벽 개방 + 수령 발판 활성화 (타이머 정지)
       ├─ 수령 발판 밟기 → 블럭스 수령하고 스폰 복귀 (런 종료)
       └─ 진행 벽 통과   → 스테이지 N+1, 타이머 20초 재시작
  → 실패 시 → 스폰 복귀. 획득 0. 패널티 없음
```

### 선택 방식 — 둘 다 3D 오브젝트
수령과 진행은 **화면 UI 버튼이 아니라 3D 오브젝트**로 처리한다.

| 선택 | 오브젝트 | 실행 조건 | 결과 |
|---|---|---|---|
| 수령 | 발판 (Part) | 밟기 (Touched) | 블럭스 수령 → 스폰 복귀. 런 종료 |
| 진행 | 벽 (Part) | 통과 | 스테이지 N+1, 타이머 재시작 |

두 선택 모두 3D 이동으로 이루어지므로 **대칭 구조**다.

**근거**
진행이 3D 이동인데 수령만 화면 버튼이면 두 선택이 같은 층위로 느껴지지 않는다.
푸시-유어-럭 구조에서는 "몸으로 선택하는" 감각이 중요하다.

**안내 표시 — BillboardGui**
발판/벽 위 안내는 BillboardGui(3D 오브젝트에 붙는 표시)로 띄운다.
화면 UI가 아니므로 시선이 월드를 벗어나지 않는다.

```
발판 = 예상 획득량   (수령 · 안전 / 노랑)
벽   = 다음 배율     (진행 · 도박 / 보라)
```

색 역할 명칭은 `docs/UI.md` "3. 색"이 기준이다 — 노랑은 **수령 · 안전**,
보라는 **진행 · 도박**. 이 이름 외의 변형 표기는 쓰지 않는다.
Part 본체 색도 같은 색을 쓴다 (발판 `#FFC61A`, 벽 `#9D4EDD`).

진행을 빨강으로 두지 않은 이유는 UI.md 같은 항목 참조 —
빨강은 경고 · 닫기 · 실패에 묶여 있어 "하지 마"로 읽히고,
그러면 유저가 진행을 안 고르게 된다. 푸시-유어-럭이 성립하려면
두 선택 다 긍정적인 색이어야 진짜로 고민한다.

**0.5초컷과의 관계**
후반 0.5초컷에서 반복되는 것은 **진행**이며, 이는 자동 진행 모드가 담당한다
(→ "시간 감각"). 수령은 런당 1회뿐이므로 발판 방식과 충돌하지 않는다.

### 푸시-유어-럭 구조
보상은 **누적이 아니라 갱신**된다.

```
스테이지 1 클리어 → 지금 나가면 1
스테이지 2 클리어 → 지금 나가면 3   (1은 대체됨)
스테이지 3 클리어 → 지금 나가면 8
스테이지 4 실패   → 0
```

### 밸런싱 원칙
성공 확률 p일 때, 더 가는 게 이득인 조건: `보상배율 > 1/p`

우리 보상 증가율은 전 구간 고정이므로 (→ 곡선 규약 3)
정지선은 게임 내내 하나다:

```
1 / 2.7 = 37%

성공률 37% 위   → 진행이 기댓값 이득
성공률 37% 아래 → 수령이 합리적
```

조절 손잡이는 보상이 아니라 **HP 곡선**이다 — 스테이지가 오를수록
성공률 p가 떨어져 정지선에 다가간다.

**구 서술("95% 구간 → 1.2배 / 75% → 1.5배 / 40% → 3배")의 설계 의도는
그대로 살아 있다.** 초반은 거의 항상 진행, 후반은 진짜 도박이라는 체감은
폐기된 것이 아니라 만드는 주체가 바뀌었다. 보상 배율이 고정이므로 이제
그 차이는 구간별 HP 가속이 성공률 p를 끌어내리면서 만든다.
손잡이가 보상에서 HP로 옮겨간 것이지 의도가 사라진 게 아니다.

이 문단이 맡는 것은 **왜 보상이 아니라 HP가 손잡이인가**(근거와 이력)까지다.
그래서 몇 %를 목표로 하는지는 아래 "목표 성공률"이 기준이다.

### 곡선 규약 (튜닝으로도 깨지 않는 계약)

아래 5개는 수치보다 상위의 계약이다. 실측 튜닝으로 값은 바뀌어도
이 관계 자체는 바뀌지 않는다.

1. **HP 증가율은 보상 증가율보다 항상 크다**
   이 관계가 뒤집히면 진행이 무조건 이득이 되어
   푸시-유어-럭이 성립하지 않는다

2. **HP 증가율은 구간별로 가속한다**
   초반은 완만하게, 후반으로 갈수록 가파르게

3. **보상 증가율은 전 구간 고정이다**
   "한 층 더 가면 N배"라는 판단 기준이 게임 내내 변하지
   않아야 유저가 예측할 수 있다. 레퍼런스도 HP만 가속하고
   보상 증가율은 2.5~2.8로 일정했다

4. **정지선 = 1 / 보상증가율**
   보상 2.7배면 성공률 37% 아래에서 멈추는 것이 합리적이다

5. **월드 전환 시 난이도는 떨어지고 보상은 오른다**
   레퍼런스 실측: 총HP 0.25x, 보상 1.67x, 벽 7개→4개
   직전 층을 겨우 뚫던 유저가 새 월드 첫 층을 한 방에
   클리어하는 승진 감각. 그 뒤 다시 가속해서 조여든다

규약 1은 `WorldConfig.validate()`가 층 단위로 검사한다. 세그먼트를
잘못 잡으면 서버 시작 시점에 터지므로 조용히 어긋날 수 없다.

곡선은 `WorldConfig.hpGrowthSegments` / `bloxGrowthSegments`로 조정한다.
튜닝 시 HP 곡선과 보상 곡선을 함께 봐야 함 — 보상만 올리면 인플레이션 발생.
**실제 수치는 코드가 기준이다. 이 문서로 복사하지 말 것.**

⚠️ 구 설정(hpGrowth 1.35 고정 + 보상 세그먼트 1.3→1.5 가속)은
보상 증가율이 HP 증가율을 앞질러 위 규약 1을 위반하고 있었다.
25층 체제 전환과 함께 HP 가속 / 보상 고정 구조로 교체했다.

현재 값은 레퍼런스 25층 실측 기반 **후보값**이다. 실측 튜닝은 힘 성장
(클릭 파워 패드 + 환생 배수)이 구현된 뒤에야 가능하다 —
HP 곡선만으로는 "20초 안에 깰 수 있는가"를 판단할 수 없다
(→ `ROADMAP.md` Phase 4-2-f).

### 목표 성공률

HP 세그먼트 구간과 1:1로 대응한다.

| 구간 | HP 증가율 | 목표 성공률 | 성격 |
|---|---|---|---|
|  1~ 8층 | 3.0x | 95% 이상 | 실패가 사고인 구간 |
|  9~16층 | 4.0x | 65~85%   | 고민이 시작되는 구간 |
| 17~25층 | 7.0x | 40~55%   | 정지선 근처. 진짜 도박 |

정지선 37%(= 1/2.7)와의 관계 —
초반은 정지선에서 한참 위라 진행이 압도적 이득이고,
후반은 정지선 바로 위라 한 층마다 진짜 판단이 된다.

#### 측정 규약

```
대상  의도한 성장 경로를 따라온 유저가 그 층에 처음 도달했을 때.
      패드를 순서대로 사고 환생을 적당히 돌린 표준 플레이어 기준.
      표준 경로 자체는 Phase 4-2-b(패드)와 4-2-d(환생)에서 확정된다

방법  시뮬레이션 우선. 표준 경로의 힘 값을 계산해 각 층 총HP와 비교한다.
      사람이 반복 플레이하는 방식은 표본이 안 나온다

오차  구간 경계에서 ±10%p까지는 조정하지 않는다
```

"처음 도달했을 때"가 핵심이다. 같은 층도 나중에 힘이 자란 뒤 다시 오면
0.5초컷이 난다 (→ "시간 감각"). 두 서술은 충돌하지 않는다 — 측정 시점이 다르다.

#### 우선순위 (셋을 동시에 만족할 수 없을 때)

```
1순위  초반 95%     깨지면 이탈한다. 절대 사수
2순위  후반 40~55%  깨지면 푸시-유어-럭이 성립하지 않는다
3순위  중반 65~85%  가장 폭이 넓다. 여기서 흡수한다
```

#### ⚠️ 20초 타이머의 특성

우리 성공률은 시간 무제한 게임과 성격이 다르다.
레퍼런스에서 성공률은 사실상 "얼마나 오래 걸리나"지만,
우리는 "20초 안에 총HP를 깎을 수 있나"의 이진 판정이다.

따라서 성공률이 계단식으로 튄다 — 힘이 조금 모자라면 0%에
가깝고 조금 넘으면 100%에 가깝다. 중간 지대가 좁다.

이를 완화하는 변수는 펀치 속도와 데미지 오버플로우 처리량이다.
같은 힘이라도 20초 동안 때리는 횟수로 총 데미지가 달라진다.
Phase 4-2에서 실측하며 감을 잡을 영역이다.

초반 목표를 90%가 아닌 95%로 잡은 이유도 이것이다 —
이진 판정 때문에 목표 90%는 실제로 60%나 100%로 튈 수 있고,
초반에 60%가 나오면 유저가 구조를 배우기 전에 이탈한다.

### 시간 감각
20초는 상한이지 라운드 길이가 아니다.
초반 18초 → 중반 4초 → 후반 0.5초로, 성장이 속도로 체감된다.

후반에 0.5초컷이 나면 진행 벽 통과를 계속 반복해야 하므로,
"자동 진행 모드"(목표 스테이지까지 멈추지 않고 통과) 필요.
반복되는 것은 진행뿐이다 — 수령은 런당 1회이므로 자동화 대상이 아니고,
발판을 밟는 방식이 후반 속도를 막지 않는다.

---

## 2. 블록

### 구조
레고 정육면체. 작은 큐브들이 격자로 조립된 형태.

```
서버 : 블록 HP 하나 + 파괴 순서 시드
클라 : HP 비율로 큐브 개수 역산 → 렌더링/제거
```

`destroyedCount = floor(총큐브수 × (1 - hp/maxHp))`
저장 불필요. HP만으로 복원 가능.

파괴 순서는 서버가 준 시드로 결정론적 셔플.
순서 배열을 통째로 전송할 필요 없음.

### 배치 (최대 16개)
```
1~4개   중앙 사각
5~8개   원형
9~16개  이중 원 (안쪽 8 + 바깥 8)
```
16칸을 미리 배치하고 비활성 칸은 숨김. 생성/제거 반복 금지.

### 데미지 오버플로우
힘은 데미지 풀이다. 블록 하나를 부수고 남으면 다음 블록으로 흘러간다.

```
블록 16개, 각 HP 10 (총 160)
  힘 25  → 블록1 파괴(10), 블록2 파괴(10), 블록3에 5 (HP 5 생존)
  힘 145 → 14개 파괴, 15번째에 5
  힘 200 → 전부 파괴. 스테이지 한 방 클리어
```

흘러가는 순서는 플레이어 위치에서 가까운 순.
전략적 의미보다 연출 때문 — 파급이 퍼지는 느낌이 난다.

반경 개념은 제거했다:
- 몇 개가 부서지는지는 이미 힘 ÷ HP로 결정된다.
  별도 개수 제한은 힘을 깎는 두 번째 손잡이일 뿐이고,
  "힘을 올렸는데 왜 안 늘지"가 된다
- R = base × log(힘)은 "힘 = 광역화" 장치였는데,
  오버플로우가 그 역할을 더 직접적으로 한다
- 힘이 지수로 커져 로그를 씌웠던 문제도, 나눗셈이라 발생하지 않는다

후반 0.5초컷은 이 구조가 있어야 성립한다.

### 재질 진행
스테이지대로 큐브 색 + 파괴 이펙트 + 사운드 변경.
```
나무 → 돌 → 철 → 크리스탈 → 용암 → 우주
```
색만 바꾸면 지루하다. 이펙트와 사운드가 바뀌어야 체감된다.
파편은 클라 전용이므로 마음껏 화려하게 해도 서버 부담 0.

---

## 3. 화폐와 배수

### 화폐 2종
- **힘** — 파괴력. 환생 시 초기화
- **블럭스** — 주 화폐. 환생 재료 + 모든 소비처

### 배수 계통 (서로 곱하지 않음)
```
[힘 계통]
환생 배수 (1000 블럭스 = +1x, 선형)
아우라 (힘 배율 + 펀치 데미지)
펫 (합산, 힘 배수 전담)
힘 배수 게임패스 (2x~1024x, 최고 단계만 적용)
힘 포션 (15분 2x)

[블럭스 계통]
2x 블럭스 게임패스
승리 포션 (15분 2x)
드론 수
```

### 환생
환생은 **소모**다. 보유 블럭스를 전액 지불하고 영구 힘 배수를 얻는다.

```
환생 실행 시
  획득 배수 = floor(현재 보유 블럭스 ÷ 1000)
  블럭스   → 0         (1000 미만 나머지도 함께 소멸. 버림)
  힘       → 초기화
  maxStage → 초기화
```

예: 1999 블럭스로 환생 → +1 배수. 999는 소멸한다.

배수는 환생할 때마다 **가산**된다. 지불한 총액에 비례하므로
성장은 지수함수가 된다. 이건 의도된 설계. 브레이크는 스테이지 게이트뿐.

⚠️ 배수를 "누적 블럭스 ÷ 1000"이라는 파생값으로 두면 안 된다.
벌기만 해도 배수가 저절로 오르므로 환생 버튼을 누를 이유가 사라지고,
환생은 힘·maxStage만 잃는 순손실 행위가 된다.
소모 방식이라 `rebirths`는 역산 불가능한 독립 필드다 (→ "10. 데이터 스키마").

```
초기화: 힘, maxStage, 블럭스
유지  : lifetimeBlox(누적 획득량), 펫, 아우라, 타이틀, 업그레이드
```

환생은 워프·아우라 롤·업그레이드와 **같은 지갑**을 놓고 경쟁한다.
"모아서 지를까, 환생할까"가 이 게임의 핵심 선택이다.

### 블럭스 소비처
```
워프      (지수)      — 즉시 진행. 전 구간 유료
환생      (선형)      — 영구 힘 배수
아우라 롤 (팩별 지수)  — 팩1 1K, 팩2 1M ... 1000배씩
타이틀 롤 (10 고정)   — 방치 진행
업그레이드            — 상점
```

워프 비용은 반드시 지수. 선형이면 후반에 무한 워프로 게임이 끝난다.
전 구간 유료가 되면서 이 조건은 더 중요해졌다.

워프는 진행 상태를 참조하지 않는다. 항상 전 구간이 열려 있고
전부 유료다. 목표 스테이지만 알면 되는 순수 함수다.

근거: `maxStage`는 환생 시 리셋된다. 워프가 이 값을 무료 구간 기준으로 쓰면
환생할 때마다 이미 지나온 구간을 지수 가격으로 다시 사게 된다.
별도 필드(`highestStageEver`)를 두는 대신, 워프에서 진행 상태 참조를
없애는 쪽을 택했다. `maxStage`는 드론 전용이 된다.

---

## 클릭 파워 패드

바닥에 패드가 여러 개 깔린다. 밟으면 클릭당 힘 증가량이 바뀐다.

패드는 **월드마다 세트가 따로 있다.** 월드에 진입하면 그 월드의 패드 세트가
맵에 깔리고, 각 세트 안에서는 누적 블럭스(`lifetimeBlox`)로 순차 해금된다.

```
월드 축         어떤 패드 세트가 존재하는가
lifetimeBlox    그 세트 안에서 어디까지 열렸는가
```

⚠️ 이 두 축을 하나로 합치지 말 것. `lifetimeBlox` 단일 축으로 구현하면
   월드 진입 시 "새 패드가 생기는" 체감이 사라진다.
   월드 상승의 핵심 보상 중 하나다.

`WorldConfig`에 패드 세트 필드가 필요하다 (필드명 미정).
실제 추가는 패드 구현 시점에 한다.

패드 위 표시는 BillboardGui — 클릭 파워 + 해금 조건.
(수령 발판·진행 벽과 동일한 3D 표시 방식)

환생해도 `lifetimeBlox`는 줄지 않으므로 패드 해금은 유지된다.

⚠️ 색은 수령 발판(노랑) · 진행 벽(보라)과 반드시 구분되어야 한다.
   두 색은 이미 의미가 배정되어 있다 — 노랑 = 수령 · 안전,
   보라 = 진행 · 도박. 색 배정 기준은 `docs/UI.md` "3. 색".

### 증가 규칙 (레퍼런스 패드 24개 실측)

```
파워 증가:  2.0x  (거의 전 구간 고정)
해금 조건:  3.0x  (평균. 2~5x 범위)
```

조건이 파워보다 빨리 오르므로 **뒤로 갈수록 패드 하나를 여는 것이 어려워진다.**
파워는 2배씩 붙는데 열쇠값은 3배씩 오르니, 격차가 패드마다 1.5배씩 벌어진다.
이것이 패드 세트 안의 브레이크다.

**월드 진입 시 첫 패드는 해금 조건 0 — 공짜로 하나 받는다.**
(레퍼런스 월드 2 첫 패드 = 조건 0)

⚠️ 월드가 오르면 패드 세트가 교체되며, 새 세트의 첫 패드는 이전 월드
   최상위 패드보다 **파워가 낮게 시작한다.** 그 대신 세트 전체의 상한이
   훨씬 높다. 잠깐 약해졌다가 훨씬 멀리 가는 구조다.

   이것을 "손해"로 읽지 말 것 — 월드 전환에서 총HP도 0.25x로 떨어지므로
   (→ "8. 월드 > 월드 전환 완급") 파워가 낮아져도 체감은 더 쉬워진다.
   두 완급은 같이 설계된 한 쌍이다.

패드 개수와 단계별 절대 수치는 미정. 위 배율만 확정이다.

※ `lifetimeBlox`의 역할이 늘었다. 기존에는 아우라 팩 해금 기준 하나였고,
  환생이 소모 방식으로 확정되면서 환생과는 무관해졌다.
  이제 **아우라 팩 해금 + 클릭 파워 패드 해금** 두 곳의 기준이다.

---

## 4. 숫자 표기

내부는 `{m, e}` = m × 10^e. 표기는 하이브리드.

```
10^3  ~ 10^32   : K M B T Qa Qi Sx Sp Oc No
10^33 ~ 10^2058 : aa ab ac ... zy zz  (676단계)
10^2061 ~       : AA AB ... ZZ        (676단계)
10^4086 이상    : 3글자 확장 (예비)
```

표시: 가수 1.00~999.99로 정규화, 소수점 2자리 고정.
접미사는 테이블이 아니라 `tier = floor(e/3)`에서 계산.

```lua
local function alphaSuffix(n)  -- n = 0부터
  local first  = math.floor(n / 26)
  local second = n % 26
  return string.char(97 + first) .. string.char(97 + second)
end
```

---

## 5. 드론

```
드론 스테이지 = maxStage - 2
드론 1대 = 60초당 해당 스테이지 보상 1회
드론 N대 = 60초당 N회 (선형)
접속 중 / 오프라인 동일 효율
챌린지 개입 없음. 순수 블럭스 채집
로벅스 전용, 상한 5대
```

오프라인 상한 8시간 (게임패스로 24시간).
상한이 리텐션 장치. 없으면 매일 접속할 이유가 사라진다.

환생 시 maxStage 초기화로 드론이 잠시 멈추지만,
3스테이지는 금방 회복되므로 무시 가능.

능동 플레이가 분당 20~60배 효율이어야 한다.
드론이 더 좋으면 능동 콘텐츠가 죽는다.

---

## 레벨

레벨 = 힘의 지수(자릿수) 기반. 상한 없음
효과 = 레벨 1당 캐릭터 최대 이동속도 +1
환생 시 힘이 초기화되므로 레벨도 함께 리셋된다

⚠️ 힘 자체가 아니라 지수를 기준으로 삼는 이유:
힘은 10^2000까지 커진다. 레벨이 힘에 비례하면 이동속도가 천문학적
숫자가 되고 로블록스 물리가 깨진다(캐릭터가 바닥을 뚫는다).
지수 기반이면 게임 전체 최대 레벨이 예측 가능한 범위에 들어온다.

```
레벨 = floor(힘의 지수 ÷ N)
N=4 라면 10^2000에서 레벨 500, 최대속도 516
                    (로블록스 기본 WalkSpeed 16 + 레벨 500)
```

N값과 레벨업 필요 수치는 미정. 밸런싱 단계에서 확정한다.

### 커스텀 스피드
유저는 자신의 최대 속도 이하 범위에서 이동속도를 자유롭게 조절할 수 있다.
기본값은 최대.

편의 기능이 아니라 **코어 루프 보호 장치**다 — 수령 발판과 진행 벽이
3D 이동으로 선택되는 구조인데, 속도가 수백이면 발판을 밟으려다
벽을 통과해버린다. 푸시-유어-럭의 선택이 사고로 뒤집힌다.

**저장하지 않는다.** 세션 내 서버 메모리에만 둔다 (→ "10. 데이터 스키마 >
저장하지 않는 것"). 접속 시와 환생 시 최대 속도로 시작한다.

저장하지 않는 이유: 한 세션 안에서도 상황에 따라 계속 바꾸는 조작값이지
한 번 정해두는 고정 설정이 아니다. 저장하면 환생·레벨 하락 시점마다
최대값 초과를 클램프해야 하는데, 세션 값이면 초기화 시점이
접속·환생 둘로 통일된다.

⚠️ 저장을 안 할 뿐 **서버 검증은 그대로 필요하다.**
   `WalkSpeed`는 클라가 바꿔도 복제되므로, 서버가 최종적으로
   `min(요청값, 최대값)`을 세팅하고 주기 검사한다.
   최대값은 레벨에서 나오고 레벨은 힘에서 파생되므로 서버가 계산한다.
   (CLAUDE.md "절대 규칙 3. 서버 권위" 적용 대상)
⚠️ 레벨이 내려가는 시점(환생)에는 세션 값도 최대를 초과할 수 있다.
   저장 여부와 무관하게 클램프는 필요하다.

---

## 캐릭터 성장 표현
힘 구간에 따라 캐릭터 체형이 단계적으로 변한다. 마른 몸 → 근육질.
힘을 키우는 게임이므로 숫자 외에 몸으로도 성장이 보여야 한다.

```
[코드]   힘 구간별 단계 판정, 모델 교체
[디자인] 단계별 캐릭터 모델
```

Phase 6 UI 작업과 함께 또는 이후. 단계 수와 구간 경계는 미정.

---

## 6. 뽑기 3종

### 공통: 행운 곱셈 체인
```
최종확률 = 기본확률 × 아우라행운 × 롤등급배수 × 게임패스 × 부스트
```
⚠️ 확률 합이 1을 넘을 수 있다. 희귀도 높은 순으로 순차 판정.

```lua
for i = #tiers, 1, -1 do
  local p = tiers[i].baseChance * totalLuck
  if random() < p then return tiers[i] end
end
return tiers[1]  -- 폴백
```

행운 업그레이드는 소폭씩만 올린다 (3.1x → 3.2x).
빠르면 롤 상품이 안 팔린다.

### 아우라
- 팩 1~5. 각 팩이 독립 테이블 (3~5종)
- 팩 해금 = 누적 블럭스 기준
- 롤 비용은 팩마다 1000배씩 증가 (1K → 1M → ...)
- 상위 팩 전 항목이 하위 팩보다 효율 우수
- 팩 선택은 유저 자유 (외형 선호 고려)
- 1개만 장착. 중첩 금지
- 중복 증발
- 자동 롤 미구현

### 타이틀
- 블럭스 10 고정 (지수 아님)
- 자동 롤 **무료 제공**
- 신규 해금 시에만 연출. 중복은 조용히 증발
- 힘 배수 + 블럭스 배수 둘 다 부여
- 1개만 장착

### 펫
- 알 뽑기. 월드가 오르면 상위 알 해금
- **힘 배수 전담**
- 장착 슬롯: 기본 2 → 업그레이드 +1 → 게임패스 +3 (최대 6)
- 장착 펫 효과는 **합산** (뽑기 3종 중 유일하게 중첩)
- 저장 공간 50 (게임패스 +100)
- 같은 펫 3마리 → 상위 등급 크래프팅
- 챌린지 중 자동 공격 (능동 보조)

⚠️ 펫 성능: 파트 1~3개로 극단적 단순화. 복잡한 리깅 금지.
   추적 이동은 클라 보간. 타인 펫 숨기기 토글 필수.

---

## 7. 업그레이드 상점 (블럭스)

```
펀치 데미지
펀치 속도      — 상한 있음
이동 속도      — 상한 있음
블럭스 획득량
아우라 행운    — 소폭씩만. 롤 상품 판매 보호
타이틀 행운    — 소폭씩만
펫 행운        — 상한 있음
펫 장착 슬롯   — 최대 +1
```
"파괴 반경" 항목 삭제 (데미지 오버플로우 도입으로 반경 개념 자체가 없어짐).
펀치 데미지와 사실상 중복이었다 — 반경이 데미지 풀 크기를 흉내내던 지표였을 뿐.

일부 항목은 로벅스 병행 구매 가능.
"맥스" 개념 필수 — 전부 무한이면 유저가 완결감을 못 느낀다.

드론 업그레이드는 이번 버전에서 제외.
드론은 상점 탭에서 로벅스로만 구매.

---

## 8. 월드

확장 가능하게만 설계. 1월드가 최우선.
월드가 올라도 추가 콘텐츠 없이 스펙과 등급만 상승.

```lua
-- WorldConfig.lua
[1] = {
  stageRange = {1, 25},
  hpBase = 100, hpGrowthSegments = { ... },   -- 구간별 가속. 실제 값은 코드 참조
  bloxBase = 1, bloxGrowthSegments = { ... }, -- 전 구간 고정. 실제 값은 코드 참조
  eggPacks = {1, 2, 3},
  auraPacks = {1, 2, 3, 4, 5},
  material = "wood",
}
-- 월드 2 추가 시 이 테이블에 항목만 붙인다
```

### 월드 1은 25층이다. 100층이 아니다.

근거:
- 100층은 유저가 한 번도 못 보고 이탈할 수 있는 거리다.
  25층이면 "월드 2 열림"이 리텐션 구간 안에 들어온다
- 커브 튜닝이 쉬워진다. 성장 칸이 24개뿐이라 HP 구간을 3개로 나눠
  잡을 수 있고, 어긋나도 어느 구간인지 바로 짚힌다.
  100층이면 세그먼트를 훨씬 잘게 썰어야 했다
- 월드 1 완성 지점이 빨리 온다. 재질 1종(나무)만 있으면 끝난다

월드 전환 조건: 25층 클리어. 별도 게이트 없음.
26층부터 자동으로 월드 2 스펙이 적용된다.

### 최종 월드의 마지막 층 — 진행 벽이 열리지 않는다

현재 `WorldConfig`에 정의된 마지막 층을 클리어하면 **진행 벽이 열리지 않는다.**
수령 발판만 활성화되고, 유저는 수령만 할 수 있다.

```
층 클리어 → 다음 층을 담당하는 월드가 WorldConfig에 있는가?
              있음 → 진행 벽 개방 + 수령 발판 활성 (평소대로)
              없음 → 수령 발판만 활성. 진행 벽 비활성
```

**이것은 임시 조치가 아니라 정식 동작이다.** 새 월드가 `WorldConfig`에 추가되면
같은 층에서 코드 수정 없이 벽이 열린다. 판정이 "25층인가"가 아니라
"다음 층을 담당하는 월드가 있는가"이기 때문이다.

25층 무한 반복도, 다음 월드 수치를 미리 정해두는 것도 택하지 않았다.
반복은 성장이 멈춘 것처럼 보이고 성공률 튜닝 때 노이즈가 된다.
근거 없는 월드 2 수치는 월드 1 실측 튜닝(→ `ROADMAP.md` Phase 3-5)보다
먼저 박히면 안 된다.

⚠️ 구현은 정상 거부 경로여야 한다. `advance()`가 없는 월드를 조회하다
   assert로 터지는 것은 버그다 — 25층 클리어는 유저의 정상 도달점이다.
   서버는 `false`를 돌려주고 사유를 로그로 남긴다.
   벽 배치 코드는 `getRunState().canAdvance`만 보면 된다.

### 월드 전환 완급 — 난이도는 떨어지고 보상은 오른다

월드가 바뀌는 순간 곡선이 **뒤로 물러난다**. 곡선 규약 5의 실제 적용이다.

```
             25층 (월드 1 끝)  →  26층 (월드 2 첫 층)
총HP              1x                    0.25x
보상              1x                    1.67x
블록(벽) 개수      7개                    4개
```

25층을 겨우 뚫던 유저가 26층을 **한 방에** 클리어한다. 이 승진 감각이
월드 상승의 핵심 보상이다 — 새 시스템도 새 화면도 추가되지 않으므로
(→ "월드 진입 시 갱신되는 것") 체감을 만드는 것은 이 완급뿐이다.

블록 개수가 함께 줄어드는 점이 중요하다. 블록HP만 낮추고 개수를 7개로
두면 총HP가 충분히 안 떨어져서 승진 감각이 나오지 않는다.
총HP = 블록HP × 블록 개수 — 두 축을 같이 내려야 0.25x가 된다.

그 뒤 다시 가속해서 조여든다. 월드 안에서 블록 개수는 초반 몇 층에 걸쳐
7개까지 회복하고, 그 뒤로는 HP 곡선이 혼자 가팔라진다.

⚠️ 블록 개수 스케줄은 지금 `StageConfig.BLOCK_COUNT_STEPS`에 있다.
   월드 2를 붙이는 시점에 월드별 값이 필요해지므로 `WorldConfig`로 옮긴다.

⚠️ "브레이크는 스테이지 게이트뿐"(→ "3. 화폐와 배수 > 환생")의 의미:
별도 관문이 있는 것이 아니라, 25층을 실제로 깰 만큼 힘이 자라야 한다는
사실 자체가 브레이크다. 환생 배수가 지수로 커져도 스테이지 HP를 넘지
못하면 진행할 수 없다.

### 월드 진입 시 갱신되는 것

```
HP · 보상 곡선      hpBase, hpGrowthSegments, bloxBase, bloxGrowthSegments
블록(벽) 개수       → "월드 전환 완급" 절 참조. 7개 → 4개로 리셋
재질                블록 색 + 파괴 이펙트 + 사운드
펫 알 팩            eggPacks — 상위 알 해금
클릭 파워 패드 세트  → "클릭 파워 패드" 절 참조
아우라 팩           auraPacks
```

새 콘텐츠(새 시스템, 새 화면)는 추가되지 않는다.
기존 시스템의 스펙과 등급만 오른다.

스테이지 번호는 월드를 넘어 연속 (1~25, 26~50).
워프·드론 로직은 월드를 몰라도 된다.

---

## 9. 수익화

### 게임패스
```
OP 자동 클리커        79
2x 환생               99
3x 부화 알            99
2x 펫 행운            99
+100 펫 저장 공간     49
2x 데미지            149
+3 펫 장착           199
2x 블럭스            199
자동 수령            199
10x 아우라 행운      299
10x 타이틀 행운      299
오프라인 24h         349
공허 훈련 구역       799
게임패스 번들      1,499  (25% 할인)
```

### 힘 배수 계단 (핵심 수익원)
```
2x      3      64x    799
4x      9     128x  1,499
8x     29     256x  2,499
16x    79     512x  3,999
32x   249    1024x  5,999
```
⚠️ 최고 단계만 적용. 누적 곱셈 금지.
3 로벅스가 첫 결제 장벽을 무너뜨리는 장치.

### 드론
```
2대 149 / 3대 299 / 4대 599 / 5대 999  (상한 5)
```

### 개발자 상품
```
5x 럭키 롤 (5x 행운)     49
15x 럭키 롤             149
5x 슈퍼 롤 (100x 행운)   99
15x 슈퍼 롤             249
힘 포션 (15분 2x)        19
승리 포션 (15분 2x)      29
포션 번들 (99+99)       999
블럭스 팩 소량           99
블럭스 팩 대량          499
워프 팩 (+10 스테이지)   199
```

### 보상형 광고
```
실패 직후     → "놓친 블럭스 절반 받기"  ← 최고의 자리
부스트 만료   → "15분 연장"
일일 무료 롤  → "슈퍼 롤 1회"
오프라인 수령 → "2배로 받기"
```
15초 이상 시청해야 수익 발생. 노필은 정상이므로 UI에서 우아하게 처리.
13세 이상에게만 노출됨.

### 무료 요소 (필수)
```
매일 무료 포션 1개 (랜덤)
자동 수령 무료판 (3초 쿨다운)
타이틀 자동 롤 (완전 무료)
```
페이월 인상을 주면 이탈한다.

### Roblox Plus 유치
환생 연출 직후 + 상점 진입 시 `PromptRobloxSubscriptionPurchase`.
월 250 로벅스 × 3개월 = 최대 750. 구현 비용 거의 0.

### 가격 원칙
- 최저가 19~49 구간 필수. 첫 결제 장벽이 가장 어렵다
- 799 이상 고가는 VIP/번들 하나로 충분
- 한정 타이머 상품은 하루 1~2회로 제한

---

## 10. 데이터 스키마

⚠️ 이 블록과 `Schema.lua`가 어긋나면 **코드가 기준이다.**
실제 템플릿은 `src/server/Data/Schema.lua`의 `buildTemplate()`이고,
여기 있는 것은 읽기용 사본이다. 필드를 바꿀 때는 코드를 먼저 고친다.

```lua
{
  schemaVersion = 2,     -- 실 코드 Schema.VERSION 과 일치시킬 것

  -- BigNum: {m = 가수, e = 지수}
  strength      = {m = 1, e = 0},
  blox          = {m = 0, e = 0},   -- 환생 시 0으로 소모됨
  lifetimeBlox  = {m = 0, e = 0},   -- 누적 획득량.
                                    --   아우라 팩 해금 + 클릭 파워 패드 해금 기준
  rebirths      = {m = 0, e = 0},   -- 누적 환생 배수. 환생마다 floor(blox/1000)만큼 가산.
                                    --   파생값 아님 — blox가 소모되므로 역산 불가

  progress = {
    maxStage = 1,        -- 환생 시 초기화. 드론 전용. 워프는 참조하지 않음
    currentWorld = 1,
    unlockedWorlds = 1,
  },

  upgrades = {
    punchDamage = 0,
    punchSpeed = 0,      -- 상한
    moveSpeed = 0,       -- 상한
    bloxGain = 0,
    auraLuck = 0,
    titleLuck = 0,
    petLuck = 0,         -- 상한
    petSlots = 0,        -- 최대 +1
  },

  drones = {
    count = 1,
    lastCollectAt = 0,   -- 서버 시각. 템플릿은 0으로 시작하고
                         --   신규 프로필 생성 시 ProfileManager가 os.time()으로 채운다
  },

  cosmetics = {
    equippedAura = false,   -- nil 아님. DataStore는 nil 값을 가진 필드를 저장하지 못한다
    ownedAuras = {},        -- {auraId = true}
    equippedTitle = false,  -- 위와 같은 이유
    ownedTitles = {},
    hideOtherAuras = false,
    hideOtherPets = false,
  },

  pets = {
    owned = {},          -- {uid = {petId, tier}}
    equipped = {},       -- uid 배열
    slots = 2,
    storage = 50,
    nextUid = 1,         -- 다음 발급할 uid 번호. 실제 uid는 tostring()으로 문자열 발급.
                         --   JSON이 숫자 키를 문자열로 바꿔 uid 참조가 어긋나는 것을 방지
  },

  rolling = {
    unlockedAuraPacks = 1,
    selectedPack = 1,
    autoRollTitle = false,
  },

  boosts = {},           -- {type, mult, expiresAt} 서버에서만 만료 판정

  daily = {
    streak = 0,
    lastClaimDay = 0,    -- UTC 일 단위 정수
    freePotionDay = 0,
  },

  stats = {
    totalRuns = 0,
    totalCashouts = 0,
    bestRunDepth = 0,
    offlineClaims = 0,
    playtimeSec = 0,
    firstJoin = 0,
  },
}
```

### 저장하지 않는 것
```
run 상태 (stage, currentReward, timeLeft)
  → 세션 내에서만. 중간 이탈 시 소멸.
     저장하면 "위험할 때 강제 종료" 익스플로잇 발생

게임패스 소유 여부
  → 매 세션 MarketplaceService 확인

커스텀 스피드
  → 세션 내에서만 유지. 접속 시·환생 시 최대 속도로 시작.
     한 세션 안에서도 상황에 따라 계속 바꾸는 조작값이라 고정 설정이 아니다.
     저장하면 환생·레벨 하락 시점마다 최대값 초과 클램프가 필요한데,
     세션 값이면 초기화 시점이 접속·환생 둘로 통일된다
```

⚠️ 펫 50~150개가 프로필에 들어간다. DataStore 4MB 한도 주의.
   펫 이름·이미지는 저장 금지. `petId`로 config 참조.

※ 삭제 이력 — `upgrades.radius`는 schemaVersion 2에서 제거됐다 (커밋 `d3a490f`).
