# 게임 설계서

## 코어 컨셉
2트랙 구조. 힘은 방치로 키우고, 블럭스는 능동 챌린지로 번다.

```
[힘 트랙 — 방치형]
클릭 / 자동 → 힘 증가 (배수 적용)
        ↓ 힘을 환전
[블럭스 트랙 — 능동]
20초 챌린지 → 블럭스 획득
```

---

## 1. 챌린지 (핵심 루프)

```
스테이지 N 진입 → 20초 타이머 시작
  → 블럭 K개를 전부 파괴하면 클리어
  → 벽 개방 + 블럭스 버튼 활성화 (타이머 정지)
       ├─ 블럭스 버튼 → 수령하고 스폰 복귀 (런 종료)
       └─ 벽 통과     → 스테이지 N+1, 타이머 20초 재시작
  → 실패 시 → 스폰 복귀. 획득 0. 패널티 없음
```

### 푸시-유어-럭 구조
보상은 **누적이 아니라 갱신**된다.

```
스테이지 1 클리어 → 지금 나가면 1
스테이지 2 클리어 → 지금 나가면 3   (1은 대체됨)
스테이지 3 클리어 → 지금 나가면 8
스테이지 4 실패   → 0
```

### 밸런싱 원칙
성공 확률 p일 때, 더 가는 게 이득인 조건: `보상배율 > 1/p`

```
성공률 95% 구간 → 배율 1.2배  (거의 항상 진행)
성공률 75% 구간 → 배율 1.5배  (진짜 고민)
성공률 40% 구간 → 배율 3배    (도박)
```

조절 손잡이는 **스테이지 HP 증가율 대비 보상 증가율** 하나.

보상 곡선은 `WorldConfig.bloxGrowthSegments`로 구간별 조정 가능.
Phase 3에서 실제 성공률 데이터를 보고 튜닝한다.
튜닝 시 HP 곡선과 보상 곡선을 함께 봐야 함 — 보상만 올리면 인플레이션 발생.

⚠️ 현재 임시값 기준 스테이지 100에서 HP 799.91T vs 보상 199.84Qa로
보상이 HP보다 3자리 이상 앞서 있음 (Qa는 T보다 1000배 큰 단위).
Phase 3 튜닝 전까지는 참고만 할 것 — 그대로 서비스하면 초반부터 인플레이션.

### 시간 감각
20초는 상한이지 라운드 길이가 아니다.
초반 18초 → 중반 4초 → 후반 0.5초로, 성장이 속도로 체감된다.

후반에 0.5초컷이 나면 블럭스 버튼을 계속 눌러야 하므로,
"자동 진행 모드"(목표 스테이지까지 멈추지 않고 통과) 필요.

---

## 2. 블록

### 구조
레고 정육면체. 작은 큐브들이 격자로 조립된 형태.

```
서버 : 블록 HP 하나 + 파괴 순서 시드
클라 : HP 비율로 큐브 개수 역산 → 렌더링/제거
```

`destroyedCount = floor(총큐브수 × (1 - hp/maxHp))`
저장 불필요. HP만으로 복원 가능.

파괴 순서는 서버가 준 시드로 결정론적 셔플.
순서 배열을 통째로 전송할 필요 없음.

### 배치 (최대 16개)
```
1~4개   중앙 사각
5~8개   원형
9~16개  이중 원 (안쪽 8 + 바깥 8)
```
16칸을 미리 배치하고 비활성 칸은 숨김. 생성/제거 반복 금지.

이중 원 구조는 파괴 반경과 잘 맞물린다.
반경이 작으면 안쪽만, 커지면 바깥까지 한 번에.

### 파괴 반경
```
R = base × log(힘)
```
로그를 쓰는 이유: 힘이 지수로 크므로 선형 매핑 시 즉시 발산.
R이 커지면 동시 타격 블럭 수가 증가. "힘 = 광역화"가 정체성.

### 재질 진행
스테이지대로 큐브 색 + 파괴 이펙트 + 사운드 변경.
```
나무 → 돌 → 철 → 크리스탈 → 용암 → 우주
```
색만 바꾸면 지루하다. 이펙트와 사운드가 바뀌어야 체감된다.
파편은 클라 전용이므로 마음껏 화려하게 해도 서버 부담 0.

---

## 3. 화폐와 배수

### 화폐 2종
- **힘** — 파괴력. 환생 시 초기화
- **블럭스** — 주 화폐. 환생 재료 + 모든 소비처

### 배수 계통 (서로 곱하지 않음)
```
[힘 계통]
환생 배수 (1000 블럭스 = +1x, 선형)
아우라 (힘 배율 + 펀치 데미지)
펫 (합산, 힘 배수 전담)
힘 배수 게임패스 (2x~1024x, 최고 단계만 적용)
힘 포션 (15분 2x)

[블럭스 계통]
2x 블럭스 게임패스
승리 포션 (15분 2x)
드론 수
```

### 환생
```
힘 배수 M = 누적 블럭스 ÷ 1000
```
선형. 배수가 누적 획득량에 비례하므로 성장은 지수함수가 된다.
이건 의도된 설계. 브레이크는 스테이지 게이트뿐.

환생 시 초기화: 힘, maxStage
유지: 블럭스 총량, 펫, 아우라, 타이틀, 업그레이드

### 블럭스 소비처
```
워프      (지수)      — 즉시 진행. 미개척 구간만 유료
환생      (선형)      — 영구 힘 배수
아우라 롤 (팩별 지수)  — 팩1 1K, 팩2 1M ... 1000배씩
타이틀 롤 (10 고정)   — 방치 진행
업그레이드            — 상점
```

워프 비용은 반드시 지수. 선형이면 후반에 무한 워프로 게임이 끝난다.
최고 도달 스테이지까지는 무료 워프, 미개척 구간만 유료.

---

## 4. 숫자 표기

내부는 `{m, e}` = m × 10^e. 표기는 하이브리드.

```
10^3  ~ 10^32   : K M B T Qa Qi Sx Sp Oc No
10^33 ~ 10^2058 : aa ab ac ... zy zz  (676단계)
10^2061 ~       : AA AB ... ZZ        (676단계)
10^4086 이상    : 3글자 확장 (예비)
```

표시: 가수 1.00~999.99로 정규화, 소수점 2자리 고정.
접미사는 테이블이 아니라 `tier = floor(e/3)`에서 계산.

```lua
local function alphaSuffix(n)  -- n = 0부터
  local first  = math.floor(n / 26)
  local second = n % 26
  return string.char(97 + first) .. string.char(97 + second)
end
```

---

## 5. 드론

```
드론 스테이지 = maxStage - 2
드론 1대 = 60초당 해당 스테이지 보상 1회
드론 N대 = 60초당 N회 (선형)
접속 중 / 오프라인 동일 효율
챌린지 개입 없음. 순수 블럭스 채집
로벅스 전용, 상한 5대
```

오프라인 상한 8시간 (게임패스로 24시간).
상한이 리텐션 장치. 없으면 매일 접속할 이유가 사라진다.

환생 시 maxStage 초기화로 드론이 잠시 멈추지만,
3스테이지는 금방 회복되므로 무시 가능.

능동 플레이가 분당 20~60배 효율이어야 한다.
드론이 더 좋으면 능동 콘텐츠가 죽는다.

---

## 6. 뽑기 3종

### 공통: 행운 곱셈 체인
```
최종확률 = 기본확률 × 아우라행운 × 롤등급배수 × 게임패스 × 부스트
```
⚠️ 확률 합이 1을 넘을 수 있다. 희귀도 높은 순으로 순차 판정.

```lua
for i = #tiers, 1, -1 do
  local p = tiers[i].baseChance * totalLuck
  if random() < p then return tiers[i] end
end
return tiers[1]  -- 폴백
```

행운 업그레이드는 소폭씩만 올린다 (3.1x → 3.2x).
빠르면 롤 상품이 안 팔린다.

### 아우라
- 팩 1~5. 각 팩이 독립 테이블 (3~5종)
- 팩 해금 = 누적 블럭스 기준
- 롤 비용은 팩마다 1000배씩 증가 (1K → 1M → ...)
- 상위 팩 전 항목이 하위 팩보다 효율 우수
- 팩 선택은 유저 자유 (외형 선호 고려)
- 1개만 장착. 중첩 금지
- 중복 증발
- 자동 롤 미구현

### 타이틀
- 블럭스 10 고정 (지수 아님)
- 자동 롤 **무료 제공**
- 신규 해금 시에만 연출. 중복은 조용히 증발
- 힘 배수 + 블럭스 배수 둘 다 부여
- 1개만 장착

### 펫
- 알 뽑기. 월드가 오르면 상위 알 해금
- **힘 배수 전담**
- 장착 슬롯: 기본 2 → 업그레이드 +1 → 게임패스 +3 (최대 6)
- 장착 펫 효과는 **합산** (뽑기 3종 중 유일하게 중첩)
- 저장 공간 50 (게임패스 +100)
- 같은 펫 3마리 → 상위 등급 크래프팅
- 챌린지 중 자동 공격 (능동 보조)

⚠️ 펫 성능: 파트 1~3개로 극단적 단순화. 복잡한 리깅 금지.
   추적 이동은 클라 보간. 타인 펫 숨기기 토글 필수.

---

## 7. 업그레이드 상점 (블럭스)

```
펀치 데미지
펀치 속도      — 상한 있음
파괴 반경
이동 속도      — 상한 있음
블럭스 획득량
아우라 행운    — 소폭씩만. 롤 상품 판매 보호
타이틀 행운    — 소폭씩만
펫 행운        — 상한 있음
펫 장착 슬롯   — 최대 +1
```

일부 항목은 로벅스 병행 구매 가능.
"맥스" 개념 필수 — 전부 무한이면 유저가 완결감을 못 느낀다.

드론 업그레이드는 이번 버전에서 제외.
드론은 상점 탭에서 로벅스로만 구매.

---

## 8. 월드

확장 가능하게만 설계. 1월드가 최우선.
월드가 올라도 추가 콘텐츠 없이 스펙과 등급만 상승.

```lua
-- WorldConfig.lua
[1] = {
  stageRange = {1, 100},
  hpBase = 100, hpGrowth = 1.35,
  bloxBase = 1, bloxGrowth = 1.4,
  eggPacks = {1, 2, 3},
  auraPacks = {1, 2, 3, 4, 5},
  material = "wood",
}
-- 월드 2 추가 시 이 테이블에 항목만 붙인다
```

스테이지 번호는 월드를 넘어 연속 (1~100, 101~200).
워프·드론 로직은 월드를 몰라도 된다.

---

## 9. 수익화

### 게임패스
```
OP 자동 클리커        79
2x 환생               99
3x 부화 알            99
2x 펫 행운            99
+100 펫 저장 공간     49
2x 데미지            149
+3 펫 장착           199
2x 블럭스            199
자동 수령            199
10x 아우라 행운      299
10x 타이틀 행운      299
오프라인 24h         349
공허 훈련 구역       799
게임패스 번들      1,499  (25% 할인)
```

### 힘 배수 계단 (핵심 수익원)
```
2x      3      64x    799
4x      9     128x  1,499
8x     29     256x  2,499
16x    79     512x  3,999
32x   249    1024x  5,999
```
⚠️ 최고 단계만 적용. 누적 곱셈 금지.
3 로벅스가 첫 결제 장벽을 무너뜨리는 장치.

### 드론
```
2대 149 / 3대 299 / 4대 599 / 5대 999  (상한 5)
```

### 개발자 상품
```
5x 럭키 롤 (5x 행운)     49
15x 럭키 롤             149
5x 슈퍼 롤 (100x 행운)   99
15x 슈퍼 롤             249
힘 포션 (15분 2x)        19
승리 포션 (15분 2x)      29
포션 번들 (99+99)       999
블럭스 팩 소량           99
블럭스 팩 대량          499
워프 팩 (+10 스테이지)   199
```

### 보상형 광고
```
실패 직후     → "놓친 블럭스 절반 받기"  ← 최고의 자리
부스트 만료   → "15분 연장"
일일 무료 롤  → "슈퍼 롤 1회"
오프라인 수령 → "2배로 받기"
```
15초 이상 시청해야 수익 발생. 노필은 정상이므로 UI에서 우아하게 처리.
13세 이상에게만 노출됨.

### 무료 요소 (필수)
```
매일 무료 포션 1개 (랜덤)
자동 수령 무료판 (3초 쿨다운)
타이틀 자동 롤 (완전 무료)
```
페이월 인상을 주면 이탈한다.

### Roblox Plus 유치
환생 연출 직후 + 상점 진입 시 `PromptRobloxSubscriptionPurchase`.
월 250 로벅스 × 3개월 = 최대 750. 구현 비용 거의 0.

### 가격 원칙
- 최저가 19~49 구간 필수. 첫 결제 장벽이 가장 어렵다
- 799 이상 고가는 VIP/번들 하나로 충분
- 한정 타이머 상품은 하루 1~2회로 제한

---

## 10. 데이터 스키마

```lua
{
  schemaVersion = 1,

  -- BigNum: {m = 가수, e = 지수}
  strength      = {m = 1, e = 0},
  blox          = {m = 0, e = 0},
  lifetimeBlox  = {m = 0, e = 0},   -- 환생 진척도
  rebirths      = {m = 0, e = 0},   -- = 힘 배수

  progress = {
    maxStage = 1,        -- 환생 시 초기화. 워프 + 드론 기준
    currentWorld = 1,
    unlockedWorlds = 1,
  },

  upgrades = {
    punchDamage = 0,
    punchSpeed = 0,      -- 상한
    radius = 0,
    moveSpeed = 0,       -- 상한
    bloxGain = 0,
    auraLuck = 0,
    titleLuck = 0,
    petLuck = 0,         -- 상한
    petSlots = 0,        -- 최대 +1
  },

  drones = {
    count = 1,
    lastCollectAt = 0,   -- 서버 시각
  },

  cosmetics = {
    equippedAura = nil,
    ownedAuras = {},     -- {auraId = true}
    equippedTitle = nil,
    ownedTitles = {},
    hideOtherAuras = false,
    hideOtherPets = false,
  },

  pets = {
    owned = {},          -- {uid = {petId, tier}}
    equipped = {},       -- uid 배열
    slots = 2,
    storage = 50,
  },

  rolling = {
    unlockedAuraPacks = 1,
    selectedPack = 1,
    autoRollTitle = false,
  },

  boosts = {},           -- {type, mult, expiresAt} 서버에서만 만료 판정

  daily = {
    streak = 0,
    lastClaimDay = 0,    -- UTC 일 단위 정수
    freePotionDay = 0,
  },

  stats = {
    totalRuns = 0,
    totalCashouts = 0,
    bestRunDepth = 0,
    offlineClaims = 0,
    playtimeSec = 0,
    firstJoin = 0,
  },
}
```

### 저장하지 않는 것
```
run 상태 (stage, currentReward, timeLeft)
  → 세션 내에서만. 중간 이탈 시 소멸.
     저장하면 "위험할 때 강제 종료" 익스플로잇 발생

게임패스 소유 여부
  → 매 세션 MarketplaceService 확인
```

⚠️ 펫 50~150개가 프로필에 들어간다. DataStore 4MB 한도 주의.
   펫 이름·이미지는 저장 금지. `petId`로 config 참조.
